PORT=5000
NODE_ENV=development
JWT_SECRET=legalease_super_secret_jwt_key_2026_production
JWT_EXPIRE=30d

# MongoDB Atlas URI (Optional: Leave empty to use zero-config local fast storage)
MONGODB_URI=

# OpenAI API Key (Optional: Dual-engine switches to built-in Legal Intelligence NLP when empty)
OPENAI_API_KEY=

# Client URL for CORS
CLIENT_URL=http://localhost:5173
